const sum = (arr) => {
  let total = 0;
  for (let i = 0; i < arr.length; i++) {
    total += arr;
  }
  return total;
};
console.log(sum([3, 2, 1]));
// running time complexity = O(n);
// space complexity = O(1)!
